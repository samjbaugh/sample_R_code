# sample_R_code

This repo contains some sample R code that I have written for three projects.

In the directory '261_final_project', 'final_project_code.R' contains the code generating the results described in 'project_write_up.pdf'.

In the directory 'Nonparametric_Final_project', 'nonparametric_final_project_code.R	' contains the code generating the results
described in 'final_project_writeup.pdf'.

In the directory 'Bikeshare', 'bikeshare.R' contains the code generating the results described in 'bikeshare.pdf'.
